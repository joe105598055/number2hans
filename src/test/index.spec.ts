import { equal } from "assert";
import number2hans from "../index";

describe("todo", function () {
  it("should todo", function () {
    const result = number2hans(1000);
    console.log(result);
    console.log(123);
    equal(true, true);
  });
});
